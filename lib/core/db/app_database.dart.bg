/*import 'package:drift/drift.dart';
import 'package:drift_flutter/drift_flutter.dart';

part 'app_database.g.dart';

class Categories extends Table {
  IntColumn get id => integer()();
  TextColumn get name => text()();
  IntColumn get order => integer().withDefault(const Constant(0))();
  @override
  Set<Column> get primaryKey => {id};
}

class Exams extends Table {
  IntColumn get id => integer()();
  TextColumn get title => text()();
  IntColumn get categoryId => integer()();
  IntColumn get questionCount => integer().withDefault(const Constant(0))();
  BoolColumn get published => boolean().withDefault(const Constant(false))();
  @override
  Set<Column> get primaryKey => {id};
}

class Attempts extends Table {
  IntColumn get id => integer().autoIncrement()();
  IntColumn get examId => integer()();
  TextColumn get mode => text()();
  DateTimeColumn get startedAt => dateTime()();
  DateTimeColumn get endedAt => dateTime().nullable()();
  BoolColumn get synced => boolean().withDefault(const Constant(false))();
}

class AttemptAnswers extends Table {
  IntColumn get id => integer().autoIncrement()();
  IntColumn get attemptId => integer()();
  IntColumn get questionId => integer()();
  TextColumn get selected => text()(); // json array of indices
  IntColumn get timeMs => integer().withDefault(const Constant(0))();
}

@DriftDatabase(tables: [Categories, Exams, Attempts, AttemptAnswers])
class AppDatabase extends _$AppDatabase {
  AppDatabase() : super(openConnection());
  @override
  int get schemaVersion => 1;
}

QueryExecutor openConnection() => driftDatabase(name: 'exampro.sqlite');*/
